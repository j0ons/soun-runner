# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Generated reports (client data — never commit)
reports/*.html
reports/*.pdf
!reports/.gitkeep

# Scan artifacts / temp
*.xml
sounrunner_*.xml

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Internal session notes / sales plan — never commit
sessionlog.md

# Machine-local settings (SMTP password, etc.) — never commit. Persists across
# `git pull`. Copy config.local.example to config.local and fill it in.
config.local

# PyInstaller build artifacts
build/
dist/
build-venv/
*.spec.bak
_sr_cleanup.bat
